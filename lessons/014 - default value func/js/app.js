const number = +prompt("enter frist number: ");
const number2 = +prompt("enter second number: ");

function sum(num1 = 0, num2 = 0){ // Default Value
    f = num1 + num2
    return f
}

alert(sum());  // <-- if we leave the () empty, the Zero and Zero will br added --> 0

